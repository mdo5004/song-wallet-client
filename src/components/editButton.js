import React from 'react';
import { Button } from 'semantic-ui-react';

const button = (props) => {
    let buttonDisplay;
    
    if (props.editing){
        buttonDisplay = <Button basic color='green' onClick={this.props.callback}>Edit</Button>;    
    }
    return (
    
        )
}